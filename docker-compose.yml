services:
  mml-redesign:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mml-redesign-app
    restart: always
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
      - PORT=3001
      - NEXT_PUBLIC_BASE_PATH=${NEXT_PUBLIC_BASE_PATH:-}
      - NEXT_PUBLIC_ASSET_URL=${NEXT_PUBLIC_ASSET_URL:-}
    volumes:
      - ./public/asset:/app/public/asset:ro
    networks:
      - mml-network

networks:
  mml-network:
    driver: bridge
